#include "main.h"

/**
 * _islower - function to check if character is lower or not
 * @c: the character to check
 *
 * Return: 1 is lower, 0 otherwise.
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
